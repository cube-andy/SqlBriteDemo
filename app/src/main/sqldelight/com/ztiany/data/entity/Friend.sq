import java.util.Calendar;

create table Friends(
   id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
   name TEXT NOT NULL ,
   city TEXT NOT NULL,
   age INTEGER AS Calendar NULL);


select_All:
select * from Friends;

select_Name:
select name from Friends;

get_Age_By_Name:
select age from Friends where name = ?;
